address 0x07fa08a855753f0ff7292fdcbe871216 {

module AAVE_USD {
    struct AAVE_USD has copy, drop, store {}
}

module BAT_USD {
    struct BAT_USD has copy, drop, store {}
}

module BTC_USD {
    struct BTC_USD has copy, drop, store {}
}

module COMP_USD {
    struct COMP_USD has copy, drop, store {}
}

module CV_INDEX {
    struct CV_INDEX has copy, drop, store {}
}

module DAI_USD {
    struct DAI_USD has copy, drop, store {}
}

module ETH_USD {
    struct ETH_USD has copy, drop, store {}
}

module GRT_ETH {
    struct GRT_ETH has copy, drop, store {}
}

module LINK_USD {
    struct LINK_USD has copy, drop, store {}
}

module MKR_USD {
    struct MKR_USD has copy, drop, store {}
}

module REP_USD {
    struct REP_USD has copy, drop, store {}
}

module SUSHI_USD {
    struct SUSHI_USD has copy, drop, store {}
}

module UNI_USD {
    struct UNI_USD has copy, drop, store {}
}

module YFI_USD {
    struct YFI_USD has copy, drop, store {}
}

module YFII_ETH {
    struct YFII_ETH has copy, drop, store {}
}

module ZRX_USD {
    struct ZRX_USD has copy, drop, store {}
}

}
